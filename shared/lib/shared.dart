library shared;

import 'package:flutter/material.dart';

class SharedWidget extends StatelessWidget {
  const SharedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Shared widget");
  }
}
